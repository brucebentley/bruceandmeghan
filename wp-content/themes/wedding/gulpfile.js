/**
 * Welcome to your gulpfile!
 *
 * The gulp tasks are split in to several files in the `gulp` directory
 * because putting them all in one file makes it way too difficult.
 */

'use strict';

var gulp = require('gulp');


